{
  "name": "@ultimate-ide/provenance",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "R2: provenance — the edit-attribution layer that connects VS Code's document model to the DSH session log. Every WorkspaceEdit carries an initiator (agent/human/extension) + agent step id, so 'who changed this line, in which turn' is queryable, replayable, and branchable.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./tracker": "./src/tracker.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ultimate-ide/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
