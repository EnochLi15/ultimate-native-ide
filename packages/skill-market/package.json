{
  "name": "@ultimate-ide/skill-market",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "R7: skill & MCP market — discovers, installs, and manages third-party agent skills (reusable instructions) and MCP servers. The open ecosystem layer that lets anyone extend the agent's capabilities.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./registry": "./src/registry.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ultimate-ide/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
